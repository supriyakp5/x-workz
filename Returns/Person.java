class Person {
    public static boolean isAlive(String name) {
        System.out.println("Checking if " + name + " is alive...");
        
        if (name=="Elon Musk")
		return true;
        if (name=="Bill Gates")
		return true;
        if (name=="Jeff Bezos") 
		return true;
        if (name."Mark Zuckerberg")
		return true;
        if (name."Sundar Pichai") 
		return true;
        if (name"Tim Cook")
		return true;
        if (name=="Warren Buffett") 
		return true;
        if (name=="Narendra Modi") 
		return true;

        return false; // If the name is not in the predefined list
    }
}